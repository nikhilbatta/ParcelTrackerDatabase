@using Parcel.Models;
@{
  Layout = "_Layout";
}

<h1>Items</h1>

@if (@Model.Count == 0)
{
  <h3>No items have been added yet!</h3>
} 

@foreach (ParcelVariable item in Model)
{
  <li>@item</li>
}

<a href="/parcels/new">Add a new parcel.</a>